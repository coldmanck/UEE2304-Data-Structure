//	List ADT Type Defintions 	typedef struct node 	   {	    void*        dataPtr;	    struct node* link;	    int grade;	    string name;	    	    int exp;	    int coef;	    int code;	    double invest;	    double amount;	    double price;	    double gain;	   } NODE; 	typedef struct	   {	    int   count; 	    NODE* pos;	    NODE* head; 	    NODE* rear;	    int    (*compare) (void* argu1, void* argu2); 	    	   } LIST;